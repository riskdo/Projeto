<section class="sobre">
  <h2>{{titulo}}</h2>

  <p>{{texto}}</p>

  <h3>Tecnologias usadas</h3>
  <ul class="tecnologias">
    {{#each tecnologias}}
      <li>{{this}}</li>
    {{/each}}
  </ul>

  <h3>Contato</h3>
  <p>{{contato.nome}} — <a href="mailto:{{contato.email}}">{{contato.email}}</a></p>
</section>
